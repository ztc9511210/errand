
server:
  port: 9990
spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://182.92.77.133:3306/communication?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    database: mysql
    hibernate:
      ddl-auto: update

eureka:
  instance:
    hostname: localhost
    instance-id: ${spring.cloud.client.ip-address}:${server.port}
    lease-renewal-interval-in-seconds: 10
    ip-address: localhost
    prefer-ip-address: true
  client:
    service-url:
#      defaultZone: http://182.92.77.133:8761/eureka
        defaultZone: http://localhost:8761/eureka
